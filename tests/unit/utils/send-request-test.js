import sendRequest from 'automation-rules/utils/send-request';
import { module, test } from 'qunit';

module('Unit | Utility | send-request', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = sendRequest();
    assert.ok(result);
  });
});
